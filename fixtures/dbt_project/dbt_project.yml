name: fastlineage_fixture
version: '1.0'
config-version: 2
profile: default

model-paths: ['models']
seed-paths: ['seeds']
snapshot-paths: ['snapshots']

target-path: 'target'
clean-targets: ['target', 'dbt_packages']
