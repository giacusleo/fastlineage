version: 2

seeds:
  - name: discount_bands
    description: "{{ doc('discount_bands__description') }}"
    config:
      column_types:
        record_id: string
        lookup_code: string
        display_name: string
        semantic_group: string
        is_active: boolean
    columns:
      - name: record_id
        description: "{{ doc('discount_bands__record_id') }}"
        data_type: string
      - name: lookup_code
        description: "{{ doc('discount_bands__lookup_code') }}"
        data_type: string
      - name: display_name
        description: "{{ doc('discount_bands__display_name') }}"
        data_type: string
      - name: semantic_group
        description: "{{ doc('discount_bands__semantic_group') }}"
        data_type: string
      - name: is_active
        description: "{{ doc('discount_bands__is_active') }}"
        data_type: boolean
