version: 2

models:
  - name: mart_support_quality
    description: "{{ doc('mart_support_quality__description') }}"
    config:
      materialized: view
    tags:
      - marts
      - presentation
      - semantic
      - mart_support_quality
    columns:
      - name: record_id
        description: "{{ doc('stg_support_tickets__record_id') }}"
        data_type: string
      - name: source_system
        description: "{{ doc('common_docs__source_system') }}"
        data_type: string
      - name: entity_name
        description: "{{ doc('common_docs__entity_name') }}"
        data_type: string
      - name: modeled_at
        description: "{{ doc('common_docs__modeled_at') }}"
        data_type: timestamp
      - name: reporting_group
        description: "{{ doc('mart_support_quality__reporting_group') }}"
        data_type: string
