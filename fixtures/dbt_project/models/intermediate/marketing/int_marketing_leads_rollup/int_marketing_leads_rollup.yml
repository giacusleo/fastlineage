version: 2

models:
  - name: int_marketing_leads_rollup
    description: "{{ doc('int_marketing_leads_rollup__description') }}"
    config:
      materialized: table
    tags:
      - intermediate
      - rollup
      - marketing
    columns:
      - name: record_id
        description: "{{ doc('stg_marketing_leads__record_id') }}"
        data_type: string
      - name: source_system
        description: "{{ doc('common_docs__source_system') }}"
        data_type: string
      - name: entity_name
        description: "{{ doc('common_docs__entity_name') }}"
        data_type: string
      - name: modeled_at
        description: "{{ doc('common_docs__modeled_at') }}"
        data_type: timestamp
      - name: model_layer
        description: "{{ doc('common_docs__model_layer') }}"
        data_type: string
